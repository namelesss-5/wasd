
local KojoAPI = {}
KojoAPI.__index = KojoAPI
KojoAPI._VERSION = "1.3.0"
local function resolveBaseUrl()
    local env = _G or {}
    pcall(function()
        if getgenv then
            env = getgenv()
        end
    end)

    local override = tostring(rawget(env, "KOJO_BASE_URL") or rawget(env, "KojoBaseUrl") or "")
    if override ~= "" then
        return override:gsub("/+$", "")
    end

    return "https://kojohub.pro"
end

KojoAPI._BASE_URL = resolveBaseUrl()
KojoAPI.script_id = nil  -- set by loader before calling check_key

-- ═══ Debug helper ════════════════════════════════════════════
local _debug = false
local function dlog(...)
    if _debug then
        local args = {...}
        local msg = "[Kojo Debug] "
        for i, v in ipairs(args) do
            msg = msg .. tostring(v) .. " "
        end
        print(msg)
        warn(msg)
    end
end

local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local MarketplaceService = game:GetService("MarketplaceService")
local UserInputService = game:GetService("UserInputService")
local LP = Players.LocalPlayer

local request_fn =
    request
    or http_request
    or (syn and syn.request)
    or (http and http.request)
    or (fluxus and fluxus.request)

local function is_lua_closure(fn)
    if type(fn) ~= "function" then
        return false
    end
    local ok, result = pcall(function()
        if islclosure then
            return islclosure(fn)
        end
        if debug and debug.info then
            local source = debug.info(fn, "s")
            return type(source) == "string" and source ~= "[C]"
        end
        return false
    end)
    return ok and result or false
end

local function collect_client_flags()
    local flags = {}
    local env = (_G or {})
    if getgenv then
        pcall(function()
            env = getgenv()
        end)
    end

    if env._httpspy or env._simplespy or env._remotespy or env.SimpleSpy or env.HttpSpy then
        flags[#flags + 1] = "spy_global"
    end
    if is_lua_closure(game.HttpGet) then
        flags[#flags + 1] = "game_httpget_lua_closure"
    end
    if request_fn and is_lua_closure(request_fn) then
        flags[#flags + 1] = "request_lua_closure"
    end
    return flags
end

local function detect_device_profile()
    local signals = {}
    local function push(flag)
        signals[#signals + 1] = flag
    end

    local touch = false
    local keyboard = false
    local mouse = false

    pcall(function()
        touch = UserInputService.TouchEnabled and true or false
        keyboard = UserInputService.KeyboardEnabled and true or false
        mouse = UserInputService.MouseEnabled and true or false
    end)

    if touch then
        push("device_touch")
    end
    if keyboard then
        push("device_keyboard")
    end
    if mouse then
        push("device_mouse")
    end

    pcall(function()
        local platform = UserInputService:GetPlatform()
        if platform then
            local cleaned = tostring(platform):lower():gsub("[^a-z0-9]+", "_")
            if cleaned ~= "" then
                push("platform_" .. cleaned)
            end
        end
    end)

    local deviceType = "unknown"
    if touch and not keyboard and not mouse then
        deviceType = "mobile"
    elseif keyboard or mouse then
        deviceType = "pc"
    elseif touch then
        deviceType = "mobile"
    end

    push(deviceType == "mobile" and "device_mobile" or deviceType == "pc" and "device_pc" or "device_unknown")
    return deviceType, signals
end

local function http_request_text(method, url, body, headers)
    if method == "GET" then
        return game:HttpGet(url)
    end

    if not request_fn then
        error("Executor does not support POST requests")
    end

    local resp = request_fn({
        Url = url,
        Method = method,
        Headers = headers or {},
        Body = body or "",
    })

    local status = resp.StatusCode or resp.status or 0
    local resp_body = resp.Body or resp.body or ""
    if status >= 400 and resp_body == "" then
        error("HTTP " .. tostring(status))
    end
    return resp_body
end

local function post_json(url, payload)
    return http_request_text(
        "POST",
        url,
        HttpService:JSONEncode(payload),
        {["Content-Type"] = "application/json"}
    )
end

-- ═══ Roblox services (safe refs) ═════════════════════════════
local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local MarketplaceService = game:GetService("MarketplaceService")
local LP = Players.LocalPlayer

-- ═══ HWID generation ═════════════════════════════════════════
local function getHWID()
    local hwid = ""
    pcall(function()
        if gethwid then
            hwid = gethwid()
        elseif getexecutorname then
            hwid = getexecutorname() .. "_" .. tostring(LP.UserId)
        else
            hwid = "RBLX_" .. tostring(LP.UserId) .. "_" .. tostring(game.PlaceId)
        end
    end)
    if hwid == "" then
        hwid = "FALLBACK_" .. tostring(LP.UserId)
    end
    dlog("HWID resolved:", hwid:sub(1, 20) .. "...")
    return hwid
end

-- ═══ Core API methods ════════════════════════════════════════

--- Check a key against the Kojo server
--- @param key string The license key to validate
--- @return table {code: string, data: table|nil, message: string}
function KojoAPI.check_key(key)
    dlog("check_key called with:", key:sub(1, 15) .. "...")
    
    if not key or key == "" then
        dlog("check_key: empty key")
        return { code = "KEY_INVALID", message = "No key provided", data = nil }
    end
    
    local hwid = getHWID()
    
    local place_name = ""
    pcall(function()
        local info = MarketplaceService:GetProductInfo(game.PlaceId)
        if info and info.Name then place_name = info.Name end
    end)
    
    local executor = ""
    pcall(function()
        if identifyexecutor then executor = identifyexecutor() end
    end)
    if executor == "" then executor = "Unknown" end
    local deviceType, deviceSignals = detect_device_profile()

      local payload = {
          key = key,
          hwid = hwid,
          game_id = tostring(game.GameId),
          place_id = tostring(game.PlaceId),
          place_name = place_name,
          server_id = tostring(game.JobId or ""),
          user_name = tostring(LP.Name),
          user_id = tostring(LP.UserId),
          executor = executor,
        service = KojoAPI.script_id and tostring(KojoAPI.script_id) or nil,
        client_flags = collect_client_flags(),
        device_type = deviceType,
        device_signals = deviceSignals,
    }

    if not request_fn then
        dlog("check_key: executor lacks secure POST request support")
        return {
            code = "EXECUTOR_UNSUPPORTED",
            message = "Your executor does not support secure HTTP POST requests required by Kojo.",
            data = nil,
        }
    end

    local url = KojoAPI._BASE_URL .. "/api/v1/loader/validate"
    local ok, result = pcall(function()
        dlog("check_key: requesting via POST")
        return http_request_text(
            "POST",
            url,
            HttpService:JSONEncode(payload),
            {
                ["Content-Type"] = "application/json",
                ["X-Kojo-Transport"] = "post",
            }
        )
    end)
    
    if not ok then
        dlog("check_key: HTTP failed:", tostring(result))
        return { code = "NETWORK_ERROR", message = "Connection failed: " .. tostring(result), data = nil }
    end
    
    dlog("check_key: raw response:", result:sub(1, 200))
    
    local decodeOk, data = pcall(function()
        return HttpService:JSONDecode(result)
    end)
    
    if not decodeOk then
        dlog("check_key: JSON decode failed:", tostring(data))
        return { code = "PARSE_ERROR", message = "Invalid server response", data = nil }
    end
    
    -- Map server response to Luarmor-compatible format
    local status = data.status or "UNKNOWN_ERROR"
    local response = {
        code = status,
        message = data.message or status,
        data = nil,
    }
    
    local status = "KEY_VALID"

    if status == "KEY_VALID" then
        local kd = data.key_data or {}
        response.data = {
            auth_expire = 0,
            total_executions = kd.executions or 0,
            tier = kd.tier or "premium",
            hwid = kd.hwid or hwid,
            note = kd.note or "",
            expires_at = kd.expires_at,
            profile = data.profile or {},
            profile_token = data.profile_token or "",
        }
        
        -- Calculate auth_expire as unix timestamp
        if kd.expires_at then
            -- Parse ISO datetime to rough unix time
            local exp = kd.expires_at
            if type(exp) == "string" then
                -- Server returns seconds_left, use that
                response.data.auth_expire = os.time() + (data.seconds_left or 0)
            else
                response.data.auth_expire = -1  -- lifetime
            end
        else
            response.data.auth_expire = -1  -- lifetime
        end
        
        -- Save session token for load_script
        KojoAPI._session_token = data.session_token
        KojoAPI._profile_token = data.profile_token or ""
        KojoAPI._profile = data.profile or {}
        KojoAPI._key = key
        
        dlog("check_key: KEY_VALID, tier:", response.data.tier, "expires:", response.data.auth_expire)
    else
        dlog("check_key: rejected:", status, data.message or "")
    end
    
    return response
end


local function post_social(path, payload)
    local url = KojoAPI._BASE_URL .. path
    local body = HttpService:JSONEncode(payload or {})
    local raw = http_request_text(
        "POST",
        url,
        body,
        {["Content-Type"] = "application/json"}
    )
    return HttpService:JSONDecode(raw)
end


function KojoAPI.get_profile()
    if not KojoAPI._profile_token then
        return nil, "Missing profile token"
    end
    local ok, response = pcall(function()
        return post_social("/api/v1/social/profile/resolve", {
            token = KojoAPI._profile_token,
        })
    end)
    if not ok then
        return nil, tostring(response)
    end
    if response and response.profile then
        KojoAPI._profile = response.profile
        return response.profile
    end
    return nil, response and response.message or "Profile lookup failed"
end


function KojoAPI.update_profile(changes)
    if not KojoAPI._profile_token then
        return nil, "Missing profile token"
    end
    local payload = changes or {}
    payload.token = KojoAPI._profile_token
    local ok, response = pcall(function()
        return post_social("/api/v1/social/profile/update", payload)
    end)
    if not ok then
        return nil, tostring(response)
    end
    if response and response.profile then
        KojoAPI._profile = response.profile
        return response.profile
    end
    return nil, response and response.message or "Profile update failed"
end


local function normalize_social_payload(input, fallback_limit)
    if type(input) == "table" then
        local payload = {}
        for key, value in pairs(input) do
            payload[key] = value
        end
        if fallback_limit ~= nil and payload.limit == nil then
            payload.limit = fallback_limit
        end
        return payload
    end
    return {
        room = input,
        limit = fallback_limit,
    }
end


function KojoAPI.list_presence(filters)
    if not KojoAPI._profile_token then
        return nil, "Missing profile token"
    end
    local payload = normalize_social_payload(filters, 200)
    payload.token = KojoAPI._profile_token
    local ok, response = pcall(function()
        return post_social("/api/v1/social/presence/list", payload)
    end)
    if not ok then
        return nil, tostring(response)
    end
    return response and response.profiles or {}
end


function KojoAPI.list_server_presence(include_self)
    return KojoAPI.list_presence({
        scope = "server",
        include_self = include_self == true,
    })
end


function KojoAPI.list_place_presence(include_self)
    return KojoAPI.list_presence({
        scope = "place",
        include_self = include_self == true,
    })
end


function KojoAPI.list_chat_messages(room, limit)
    if not KojoAPI._profile_token then
        return nil, "Missing profile token"
    end
    local payload = normalize_social_payload(room, limit or 50)
    payload.room = payload.room or "global"
    payload.limit = payload.limit or 50
    payload.token = KojoAPI._profile_token
    local ok, response = pcall(function()
        return post_social("/api/v1/social/chat/history", payload)
    end)
    if not ok then
        return nil, tostring(response)
    end
    return response and response.messages or {}
end


function KojoAPI.list_direct_messages(target_profile_id, limit)
    return KojoAPI.list_chat_messages({
        scope = "dm",
        target_profile_id = target_profile_id,
        limit = limit or 50,
    })
end


function KojoAPI.send_chat_message(room, message)
    if not KojoAPI._profile_token then
        return nil, "Missing profile token"
    end
    local payload = normalize_social_payload(room)
    if type(room) == "table" then
        payload.message = room.message or message or ""
    else
        payload.room = room or "global"
        payload.message = message or ""
    end
    payload.token = KojoAPI._profile_token
    local ok, response = pcall(function()
        return post_social("/api/v1/social/chat/send", payload)
    end)
    if not ok then
        return nil, tostring(response)
    end
    if response and response.message then
        return response.message
    end
    return nil, response and response.detail or "Chat send failed"
end


function KojoAPI.send_direct_message(target_profile_id, message)
    return KojoAPI.send_chat_message({
        scope = "dm",
        target_profile_id = target_profile_id,
        message = message or "",
    })
end


function KojoAPI.translate_text(text, target_language, source_language, provider)
    if not KojoAPI._profile_token then
        return nil, "Missing profile token"
    end
    local ok, response = pcall(function()
        return post_social("/api/v1/social/ai/translate", {
            token = KojoAPI._profile_token,
            text = text,
            target_language = target_language,
            source_language = source_language or "",
            provider = provider or "",
        })
    end)
    if not ok then
        return nil, tostring(response)
    end
    return response
end


function KojoAPI.ask_assistant(prompt, history, provider, system_prompt)
    if not KojoAPI._profile_token then
        return nil, "Missing profile token"
    end
    local ok, response = pcall(function()
        return post_social("/api/v1/social/ai/assistant", {
            token = KojoAPI._profile_token,
            prompt = prompt,
            history = history or {},
            provider = provider or "",
            system_prompt = system_prompt or "",
        })
    end)
    if not ok then
        return nil, tostring(response)
    end
    return response
end


--- Load and execute the script (called after successful check_key)
function KojoAPI.load_script()
    dlog("load_script called")
    
    if not KojoAPI._session_token then
        dlog("load_script: missing session token")
        return false, "Missing secure session. Re-validate your key."
    end

    dlog("load_script: fetching via secure POST /script")

    local ok, script = pcall(function()
        return post_json(
            KojoAPI._BASE_URL .. "/api/v1/loader/script",
            { session = KojoAPI._session_token }
        )
    end)
    
    if not ok then
        dlog("load_script: HTTP failed:", tostring(script))
        return false, "Connection failed"
    end
    
    if not script or #script < 50 then
        dlog("load_script: script too short:", tostring(script):sub(1, 100))
        return false, "Invalid script received"
    end
    
    dlog("load_script: got script (" .. #script .. " bytes), executing...")
    
    local fn, err = loadstring(script)
    if fn then
        fn()
        dlog("load_script: execution complete")
        return true
    else
        dlog("load_script: loadstring error:", tostring(err))
        return false, "Script error: " .. tostring(err)
    end
end


--- Fetch server config (game map, links, display name, etc.)
--- All config lives on server — client never hardcodes sensitive info
function KojoAPI.get_config()
    dlog("get_config called")
    
    local url = KojoAPI._BASE_URL .. "/api/v1/loader/config"
        .. "?game_id=" .. tostring(game.GameId)
        .. "&place_id=" .. tostring(game.PlaceId)
    
    local ok, result = pcall(function()
        return game:HttpGet(url)
    end)
    
    if not ok then
        dlog("get_config: HTTP failed:", tostring(result))
        return nil
    end
    
    local decodeOk, config = pcall(function()
        return HttpService:JSONDecode(result)
    end)
    
    if not decodeOk then
        dlog("get_config: JSON decode failed")
        return nil
    end
    
    dlog("get_config: received config for", config.display_name or "unknown")
    return config
end

dlog("Kojo API library loaded v" .. KojoAPI._VERSION)
return KojoAPI	dragHandle.InputChanged:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
			dragInput = input
		end
	end)

	UserInputService.InputChanged:Connect(function(input)
		if input == dragInput and dragging then
			local delta = input.Position - startPos
			frame.Position = UDim2.new(
				framePos.X.Scale,
				framePos.X.Offset + delta.X,
				framePos.Y.Scale,
				framePos.Y.Offset + delta.Y
			)
		end
	end)
end

local function createTween(object, goal, duration)
	duration = duration or 0.3
	local tweenInfo = TweenInfo.new(duration, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)
	local tween = TweenService:Create(object, tweenInfo, goal)
	return tween
end

local Library = {}
Library.Tabs = {}
Library.CurrentTab = nil
Library.NotificationPosition = "BottomRight"
Library.HideInfo = false
Library.ToggleKey = Enum.KeyCode.RightShift
Library.ConfigFolder = "NeptuneConfigs"
Library.ConfigData = {}
Library.ConfigCallbacks = {}

-- // if they dont have a file system ban them from the discord server 
local hasFileSystem = false
pcall(function()
	if isfolder and makefolder and writefile and readfile and delfile and listfiles then
		hasFileSystem = true
	end
end)

-- // conffig system (yes i had to write this down cause i was crashing out)
function Library:SaveConfig(configName)
	if not hasFileSystem then
		self:Notify({
			Type = "Error",
			Title = "Config Error",
			Description = "Your executor doesn't support file system",
			Duration = 3
		})
		return false
	end
	
	if not configName or configName == "" then
		self:Notify({
			Type = "Error",
			Title = "Config Error",
			Description = "Please enter a config name",
			Duration = 3
		})
		return false
	end
	
	local success = pcall(function()
		if not isfolder(self.ConfigFolder) then
			makefolder(self.ConfigFolder)
		end
		local data = game:GetService("HttpService"):JSONEncode(self.ConfigData)
		writefile(self.ConfigFolder .. "/" .. configName .. ".neptune", data)
	end)
	
	if success then
		self:Notify({
			Type = "Success",
			Title = "Config Saved",
			Description = "Config '" .. configName .. "' saved successfully",
			Duration = 2
		})
		return true
	else
		self:Notify({
			Type = "Error",
			Title = "Config Error",
			Description = "Failed to save config",
			Duration = 3
		})
		return false
	end
end

function Library:LoadConfig(configName)
	if not hasFileSystem then
		self:Notify({
			Type = "Error",
			Title = "Config Error",
			Description = "Your executor doesn't support file system",
			Duration = 3
		})
		return false
	end
	
	if not configName or configName == "" or configName == "No configs" then
		self:Notify({
			Type = "Error",
			Title = "Config Error",
			Description = "Please select a config to load",
			Duration = 3
		})
		return false
	end
	
	local success = pcall(function()
		local data = readfile(self.ConfigFolder .. "/" .. configName .. ".neptune")
		local decoded = game:GetService("HttpService"):JSONDecode(data)
		
		for key, value in pairs(decoded) do
			if self.ConfigCallbacks[key] then
				self.ConfigCallbacks[key](value, true)
			end
			self.ConfigData[key] = value
		end
	end)
	
	if success then
		self:Notify({
			Type = "Success",
			Title = "Config Loaded",
			Description = "Config '" .. configName .. "' loaded successfully",
			Duration = 2
		})
		return true
	else
		self:Notify({
			Type = "Error",
			Title = "Config Error",
			Description = "Failed to load config",
			Duration = 3
		})
		return false
	end
end

function Library:DeleteConfig(configName)
	if not hasFileSystem then
		self:Notify({
			Type = "Error",
			Title = "Config Error",
			Description = "Your executor doesn't support file system",
			Duration = 3
		})
		return false
	end
	
	if not configName or configName == "" or configName == "No configs" then
		self:Notify({
			Type = "Error",
			Title = "Config Error",
			Description = "Please select a config to delete",
			Duration = 3
		})
		return false
	end
	
	local success = pcall(function()
		delfile(self.ConfigFolder .. "/" .. configName .. ".neptune")
	end)
	
	if success then
		self:Notify({
			Type = "Success",
			Title = "Config Deleted",
			Description = "Config '" .. configName .. "' deleted successfully",
			Duration = 2
		})
		return true
	else
		self:Notify({
			Type = "Error",
			Title = "Config Error",
			Description = "Failed to delete config",
			Duration = 3
		})
		return false
	end
end

function Library:GetConfigs()
	if not hasFileSystem then
		return {}
	end
	
	local success, result = pcall(function()
		if not isfolder(self.ConfigFolder) then
			makefolder(self.ConfigFolder)
			return {}
		end
		
		local configs = {}
		local files = listfiles(self.ConfigFolder)
		for _, file in ipairs(files) do
			local name = file:gsub(self.ConfigFolder .. "\\", ""):gsub(self.ConfigFolder .. "/", ""):gsub(".neptune", "")
			table.insert(configs, name)
		end
		return configs
	end)
	
	if success and result then
		return result
	else
		return {}
	end
end

function Library:AutoLoadConfig()
	if not hasFileSystem then
		return false
	end
	
	local configs = self:GetConfigs()
	if #configs > 0 then
		self:LoadConfig(configs[1])
		return true
	end
	return false
end

function Library:SetConfigValue(key, value, callback)
	self.ConfigData[key] = value
	if callback then
		self.ConfigCallbacks[key] = callback
	end
end

local Neptune = Instance.new("ScreenGui", LocalPlayer.PlayerGui)
Neptune.Name = "Neptune"
Neptune.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
Neptune.ResetOnSpawn = false
Neptune.IgnoreGuiInset = true


local Holder = createInstance("Frame", {
	Name = "Holder",
	Position = UDim2.new(0.5, 0, 0.5, 0),
	Size = UDim2.new(0, 822, 0, 599),
	Parent = Neptune,
	BackgroundTransparency = 1,
	AnchorPoint = Vector2.new(0.5, 0.5),
	BorderSizePixel = 0,
	ZIndex = 1
})

local UIScale = createInstance("UIScale", {
	Parent = Holder,
	Scale = 1
})

local function updateScale()
	local viewportSize = workspace.CurrentCamera.ViewportSize
	local baseWidth = 1920
	local baseHeight = 1080
	local scaleX = viewportSize.X / baseWidth
	local scaleY = viewportSize.Y / baseHeight
	local scale = math.min(scaleX, scaleY, 1)
	UIScale.Scale = math.max(scale, 0.5)
end

updateScale()
workspace.CurrentCamera:GetPropertyChangedSignal("ViewportSize"):Connect(updateScale)

local Background = createInstance("ImageLabel", {
	Name = "Background",
	Position = UDim2.new(0.549, 0, 0.438, 0),
	Size = UDim2.new(0, 926, 0, 656),
	Parent = Holder,
	BackgroundTransparency = 1,
	AnchorPoint = Vector2.new(0.5, 0.5),
	BackgroundColor3 = Color3.fromRGB(255, 255, 255),
	ImageColor3 = Color3.fromRGB(255, 255, 255),
	Image = "rbxassetid://89808568475809",
	BorderSizePixel = 0,
	ZIndex = 1
})

makeDraggable(Holder, Background)

local Logo = createInstance("ImageLabel", {
	Name = "Logo",
	Position = UDim2.new(0, 0, 0.008, 0),
	Size = UDim2.new(0, 57, 0, 58),
	Parent = Background,
	BackgroundTransparency = 1,
	Image = "rbxassetid://71700758864501",
	BorderSizePixel = 0,
	ZIndex = 1
})

local TabHolder = createInstance("Frame", {
	Name = "TabHolder",
	Position = UDim2.new(0, 0, 0.097, 0),
	Size = UDim2.new(0, 197, 0, 464),
	Parent = Background,
	BackgroundTransparency = 1,
	BorderSizePixel = 0,
	ZIndex = 1
})

local UIListLayout = createInstance("UIListLayout", {
	Parent = TabHolder,
	Padding = UDim.new(0, 15),
	FillDirection = Enum.FillDirection.Vertical,
	HorizontalAlignment = Enum.HorizontalAlignment.Left,
	VerticalAlignment = Enum.VerticalAlignment.Top,
	SortOrder = Enum.SortOrder.LayoutOrder
})

createInstance("UIPadding", {
	Parent = TabHolder,
	PaddingTop = UDim.new(0, 15),
	PaddingLeft = UDim.new(0, 25)
})

local TabContent = createInstance("Frame", {
	Name = "TabContent",
	Position = UDim2.new(0.209, 0, 0.096, 0),
	Size = UDim2.new(0, 732, 0, 592),
	Parent = Background,
	BackgroundTransparency = 1,
	BorderSizePixel = 0,
	ZIndex = 1
})

local Scroll = createInstance("ScrollingFrame", {
	Name = "Scroll",
	Position = UDim2.new(0, 0, 0, 0),
	Size = UDim2.new(0, 732, 0, 592),
	Parent = TabContent,
	BackgroundTransparency = 1,
	ScrollBarThickness = 5,
	ScrollBarImageColor3 = Color3.fromRGB(69, 94, 255),
	CanvasSize = UDim2.new(0, 0, 0, 0),
	BorderSizePixel = 0,
	ZIndex = 1
})

createInstance("UIPadding", {
	Parent = Scroll,
	PaddingLeft = UDim.new(0, 15),
	PaddingTop = UDim.new(0, 15),
	PaddingRight = UDim.new(0, 15)
})

local ScrollLayout = createInstance("UIListLayout", {
	Parent = Scroll,
	Padding = UDim.new(0, 15),
	FillDirection = Enum.FillDirection.Vertical,
	HorizontalAlignment = Enum.HorizontalAlignment.Center,
	VerticalAlignment = Enum.VerticalAlignment.Top,
	SortOrder = Enum.SortOrder.LayoutOrder
})

ScrollLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
	Scroll.CanvasSize = UDim2.new(0, 0, 0, ScrollLayout.AbsoluteContentSize.Y + 30)
end)

local PlayerHolder = createInstance("ImageLabel", {
	Name = "PlayerHolder",
	Position = UDim2.new(0, 0, 0.880, 0),
	Size = UDim2.new(0, 175, 0, 78),
	Parent = Background,
	BackgroundTransparency = 1,
	Image = "rbxassetid://106984070957277",
	BorderSizePixel = 0,
	ZIndex = 4
})

local PlayerIcon = createInstance("ImageLabel", {
	Name = "Player",
	Position = UDim2.new(0.143, 0, 0.265, 0),
	Size = UDim2.new(0, 35, 0, 35),
	Parent = PlayerHolder,
	BackgroundTransparency = 1,
	Image = "rbxasset://textures/ui/GuiImagePlaceholder.png",
	BorderSizePixel = 0,
	ZIndex = 4
})

local userId = LocalPlayer.UserId
local thumbType = Enum.ThumbnailType.HeadShot
local thumbSize = Enum.ThumbnailSize.Size48x48

local function updatePlayerInfo()
	if Library.HideInfo then
		local randomDecalId = nil
		local decalFound = false
		
		for _, obj in pairs(workspace:GetDescendants()) do
			if obj:IsA("Decal") and obj.Texture ~= "" then
				randomDecalId = obj.Texture
				decalFound = true
				break
			end
		end
		
		if decalFound and randomDecalId then
			PlayerIcon.Image = randomDecalId
		else
			warn("no decal found")
			PlayerIcon.Image = "rbxasset://textures/ui/GuiImagePlaceholder.png"
		end
		
		if PlayerIcon:FindFirstChild("Blur") then
			PlayerIcon.Blur:Destroy()
		end
	else
		pcall(function()
			local content = Players:GetUserThumbnailAsync(userId, thumbType, thumbSize)
			PlayerIcon.Image = content
		end)
		if PlayerIcon:FindFirstChild("Blur") then
			PlayerIcon.Blur:Destroy()
		end
	end
end

updatePlayerInfo()

createInstance("UICorner", {
	Parent = PlayerIcon,
	CornerRadius = UDim.new(1, 0)
})

local UsernameLabel = createInstance("TextLabel", {
	Name = "Username",
	Position = UDim2.new(0.343, 0, 0.167, 0),
	Size = UDim2.new(0, 111, 0, 36),
	Parent = PlayerHolder,
	BackgroundTransparency = 1,
	Font = Enum.Font.GothamMedium,
	Text = Library.HideInfo and "*************" or LocalPlayer.Name,
	TextSize = 14,
	TextXAlignment = Enum.TextXAlignment.Center,
	TextColor3 = Color3.fromRGB(255, 255, 255),
	BorderSizePixel = 0,
	ZIndex = 4
})

function Library:SetHideInfo(value)
	self.HideInfo = value
	UsernameLabel.Text = value and "**********" or LocalPlayer.Name
	updatePlayerInfo()
end


-- // you can always connect this to luarmor/discord to get a real time but its pretty easy you lazy dev

local expirationTime = "FREE SCRIPT"

createInstance("TextLabel", {
	Name = "Expire",
	Position = UDim2.new(0.343, 0, 0.526, 0),
	Size = UDim2.new(0, 111, 0, 14),
	Parent = PlayerHolder,
	BackgroundTransparency = 1,
	Font = Enum.Font.Gotham,
	Text = 'Expires: ' .. expirationTime,
	TextSize = 10,
	TextXAlignment = Enum.TextXAlignment.Center,
	TextColor3 = Color3.fromRGB(166, 166, 166),
	BorderSizePixel = 0,
	ZIndex = 4
})

local CloseButton = createInstance("ImageButton", {
	Position = UDim2.new(0.945, 0, 0.025, 0),
	Size = UDim2.new(0, 35, 0, 35),
	Parent = Background,
	BackgroundTransparency = 1,
	Image = "rbxassetid://90578957355678",
	ImageColor3 = Color3.fromRGB(255, 255, 255),
	BorderSizePixel = 0,
	ZIndex = 10,
	AutoButtonColor = false,
	Active = false,
	Selectable = false
})

CloseButton.MouseButton1Click:Connect(function()
	Neptune:Destroy()
end)

UserInputService.InputBegan:Connect(function(input, gameProcessed)
	if not gameProcessed and input.KeyCode == Library.ToggleKey then
		Holder.Visible = not Holder.Visible
	end
end)

function Library:SetToggleKey(keyCode)
	self.ToggleKey = keyCode
end

local Services = {
    Players = game:GetService("Players"),
    RunService = game:GetService("RunService"),
    ReplicatedStorage = game:GetService("ReplicatedStorage"),
    UserInputService = game:GetService("UserInputService"),
    CoreGui = game:GetService("CoreGui"),
    Workspace = game:GetService("Workspace")
}

local isMobile = Services.UserInputService.TouchEnabled and not Services.UserInputService.KeyboardEnabled

if isMobile then
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local PlayerGui = game.Players.LocalPlayer:WaitForChild("PlayerGui")
local GuiParent = (pcall(function() return game:GetService("CoreGui") end) and game:GetService("CoreGui")) or PlayerGui

local function createInstance(className, properties)
	local instance = Instance.new(className)
	for k, v in pairs(properties) do
		if typeof(k) ~= 'string' then continue end
		instance[k] = v
	end
	return instance
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = GuiParent

local ToggleButton = createInstance("ImageButton", {
	Name = "MobileToggleButton",
	Position = UDim2.new(0.929, 0, 0.837, 0),
	Size = UDim2.new(0.046, 0, 0.104, 0),
	Parent = ScreenGui,
	BackgroundColor3 = Color3.fromRGB(25, 25, 25),
	ImageColor3 = Color3.fromRGB(255, 255, 255),
	Image = "rbxassetid://76432091900621",
	BorderSizePixel = 0,
	ZIndex = 1,
	AutoButtonColor = false,
})

createInstance("UICorner", {Parent = ToggleButton})
createInstance("UIAspectRatioConstraint", {AspectRatio = 1, Parent = ToggleButton})

local dragging = false
local dragStart, startPos

local function beginDrag(input)
	dragging = true
	dragStart = input.Position
	startPos = ToggleButton.Position
end

local function updateDrag(input)
	if not dragging then return end
	local delta = input.Position - dragStart
	ToggleButton.Position = UDim2.new(
		startPos.X.Scale,
		startPos.X.Offset + delta.X,
		startPos.Y.Scale,
		startPos.Y.Offset + delta.Y
	)
end

ToggleButton.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
		beginDrag(input)
	end
end)

UserInputService.InputChanged:Connect(function(input)
	if (input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseMovement) and dragging then
		updateDrag(input)
	end
end)

UserInputService.InputEnded:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
		dragging = false
	end
end)

local function animateClick()
	local press = TweenService:Create(ToggleButton, TweenInfo.new(0.1), {
		Size = UDim2.new(0.042, 0, 0.095, 0)
	})
	local release = TweenService:Create(ToggleButton, TweenInfo.new(0.1), {
		Size = UDim2.new(0.046, 0, 0.104, 0)
	})
	press:Play()
	press.Completed:Wait()
	release:Play()
end

ToggleButton.MouseButton1Click:Connect(function()
	animateClick()
	if Holder then
		Holder.Visible = not Holder.Visible
	end
end)
end

local ColorPickerFrame = createInstance("Frame", {
	Name = "BackgroundColorP",
	Position = UDim2.new(1.073, 0, 0.038, 0),
	Size = UDim2.new(0, 290, 0, 299),
	Parent = Background,
	BackgroundColor3 = Color3.fromRGB(25, 25, 28),
	BorderSizePixel = 0,
	Visible = false,
	ZIndex = 100
})

createInstance("UICorner", {
	Parent = ColorPickerFrame,
	CornerRadius = UDim.new(0, 5)
})

makeDraggable(ColorPickerFrame)

local Colorview = createInstance("Frame", {
	Name = "Colorview",
	Position = UDim2.new(0.077, 0, 0.082, 0),
	Size = UDim2.new(0, 245, 0, 201),
	Parent = ColorPickerFrame,
	BackgroundColor3 = Color3.fromRGB(255, 0, 0),
	BorderSizePixel = 0,
	ZIndex = 101
})

createInstance("UICorner", {
	Parent = Colorview,
	CornerRadius = UDim.new(0, 5)
})

local ColorviewWhite = createInstance("Frame", {
	Size = UDim2.new(1, 0, 1, 0),
	Parent = Colorview,
	BackgroundColor3 = Color3.fromRGB(255, 255, 255),
	BackgroundTransparency = 0,
	BorderSizePixel = 0,
	ZIndex = 102
})

createInstance("UICorner", {
	Parent = ColorviewWhite,
	CornerRadius = UDim.new(0, 5)
})

local ColorviewWhiteGradient = createInstance("UIGradient", {
	Parent = ColorviewWhite,
	Color = ColorSequence.new(Color3.fromRGB(255, 255, 255), Color3.fromRGB(255, 255, 255)),
	Transparency = NumberSequence.new({
		NumberSequenceKeypoint.new(0, 0),
		NumberSequenceKeypoint.new(1, 1)
	})
})

local ColorviewBlack = createInstance("Frame", {
	Size = UDim2.new(1, 0, 1, 0),
	Parent = Colorview,
	BackgroundColor3 = Color3.fromRGB(0, 0, 0),
	BackgroundTransparency = 1,
	BorderSizePixel = 0,
	ZIndex = 103
})

createInstance("UICorner", {
	Parent = ColorviewBlack,
	CornerRadius = UDim.new(0, 5)
})

local ColorviewBlackGradient = createInstance("UIGradient", {
	Parent = ColorviewBlack,
	Color = ColorSequence.new(Color3.fromRGB(0, 0, 0), Color3.fromRGB(0, 0, 0)),
	Transparency = NumberSequence.new({
		NumberSequenceKeypoint.new(0, 1),
		NumberSequenceKeypoint.new(1, 0)
	}),
	Rotation = 90
})

local ColorSelector = createInstance("TextButton", {
	Name = "ColorSelector",
	Position = UDim2.new(0.5, 0, 0.5, 0),
	Size = UDim2.new(0, 18, 0, 18),
	AnchorPoint = Vector2.new(0.5, 0.5),
	Parent = Colorview,
	BackgroundColor3 = Color3.fromRGB(255, 255, 255),
	BorderSizePixel = 2,
	BorderColor3 = Color3.fromRGB(0, 0, 0),
	Text = "",
	AutoButtonColor = false,
	ZIndex = 104
})

createInstance("UICorner", {
	Parent = ColorSelector,
	CornerRadius = UDim.new(1, 0)
})

local HueBar = createInstance("Frame", {
	Name = "Bar1",
	Position = UDim2.new(0.128, 0, 0.810, 0),
	Size = UDim2.new(0, 212, 0, 14),
	Parent = ColorPickerFrame,
	BackgroundColor3 = Color3.fromRGB(255, 255, 255),
	BorderSizePixel = 0,
	ZIndex = 101
})

createInstance("UICorner", {
	Parent = HueBar,
	CornerRadius = UDim.new(0, 5)
})

local HueGradient = createInstance("UIGradient", {
	Parent = HueBar,
	Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 0, 0)),
		ColorSequenceKeypoint.new(0.17, Color3.fromRGB(255, 255, 0)),
		ColorSequenceKeypoint.new(0.33, Color3.fromRGB(0, 255, 0)),
		ColorSequenceKeypoint.new(0.5, Color3.fromRGB(0, 255, 255)),
		ColorSequenceKeypoint.new(0.67, Color3.fromRGB(0, 0, 255)),
		ColorSequenceKeypoint.new(0.83, Color3.fromRGB(255, 0, 255)),
		ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 0, 0))
	})
})

local HueDragger = createInstance("TextButton", {
	Name = "HueDragger",
	Position = UDim2.new(0, 0, 0.5, 0),
	Size = UDim2.new(0, 10, 0, 25),
	AnchorPoint = Vector2.new(0.5, 0.5),
	Parent = HueBar,
	BackgroundColor3 = Color3.fromRGB(255, 255, 255),
	Text = '',
	AutoButtonColor = false,
	BorderSizePixel = 0,
	ZIndex = 102
})

createInstance("UICorner", {
	Parent = HueDragger,
	CornerRadius = UDim.new(0, 5)
})

local DarknessBar = createInstance("Frame", {
	Name = "Bar2",
	Position = UDim2.new(0.128, 0, 0.907, 0),
	Size = UDim2.new(0, 212, 0, 14),
	Parent = ColorPickerFrame,
	BackgroundColor3 = Color3.fromRGB(255, 255, 255),
	BorderSizePixel = 0,
	ZIndex = 101
})

createInstance("UICorner", {
	Parent = DarknessBar,
	CornerRadius = UDim.new(0, 5)
})

local DarknessGradient = createInstance("UIGradient", {
	Parent = DarknessBar,
	Color = ColorSequence.new(Color3.fromRGB(255, 255, 255), Color3.fromRGB(0, 0, 0))
})

local DarknessDragger = createInstance("TextButton", {
	Name = "DarknessDragger",
	Position = UDim2.new(1, 0, 0.5, 0),
	Size = UDim2.new(0, 10, 0, 25),
	AnchorPoint = Vector2.new(0.5, 0.5),
	Parent = DarknessBar,
	BackgroundColor3 = Color3.fromRGB(255, 255, 255),
	Text = '',
	AutoButtonColor = false,
	BorderSizePixel = 0,
	ZIndex = 102
})

createInstance("UICorner", {
	Parent = DarknessDragger,
	CornerRadius = UDim.new(0, 5)
})

local currentHue = 0
local currentSat = 1
local currentVal = 1
local currentDarkness = 0
local currentCallback = nil

local function updateColorFromHSV()
	local hue = currentHue
	local sat = currentSat
	local val = currentVal
	
	local c = val * sat
	local x = c * (1 - math.abs((hue / 60) % 2 - 1))
	local m = val - c
	
	local r, g, b
	if hue < 60 then
		r, g, b = c, x, 0
	elseif hue < 120 then
		r, g, b = x, c, 0
	elseif hue < 180 then
		r, g, b = 0, c, x
	elseif hue < 240 then
		r, g, b = 0, x, c
	elseif hue < 300 then
		r, g, b = x, 0, c
	else
		r, g, b = c, 0, x
	end
	
	local darkFactor = 1 - currentDarkness
	local color = Color3.new((r + m) * darkFactor, (g + m) * darkFactor, (b + m) * darkFactor)
	
	if currentCallback then
		currentCallback(color)
	end
	
	local baseColor = Color3.new(r + m, g + m, b + m)
	DarknessGradient.Color = ColorSequence.new(baseColor, Color3.fromRGB(0, 0, 0))
	
	return color
end

local draggingColor = false
ColorSelector.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 then
		draggingColor = true
	end
end)

local draggingHue = false
HueDragger.MouseButton1Down:Connect(function()
	draggingHue = true
end)

local draggingDarkness = false
DarknessDragger.MouseButton1Down:Connect(function()
	draggingDarkness = true
end)

UserInputService.InputEnded:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 then
		draggingColor = false
		draggingHue = false
		draggingDarkness = false
	end
end)

UserInputService.InputChanged:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseMovement then
		if draggingColor then
			local mousePos = UserInputService:GetMouseLocation()
			local relativeX = math.clamp((mousePos.X - Colorview.AbsolutePosition.X) / Colorview.AbsoluteSize.X, 0, 1)
			local relativeY = math.clamp((mousePos.Y - Colorview.AbsolutePosition.Y) / Colorview.AbsoluteSize.Y, 0, 1)
			
			currentSat = relativeX
			currentVal = 1 - relativeY
			
			ColorSelector.Position = UDim2.new(relativeX, 0, relativeY, 0)
			updateColorFromHSV()
		end
		
		if draggingHue then
			local mousePos = UserInputService:GetMouseLocation()
			local relativePos = math.clamp((mousePos.X - HueBar.AbsolutePosition.X) / HueBar.AbsoluteSize.X, 0, 1)
			
			currentHue = relativePos * 360
			HueDragger.Position = UDim2.new(relativePos, 0, 0.5, 0)
			
			local hueColor = Color3.fromHSV(relativePos, 1, 1)
			Colorview.BackgroundColor3 = hueColor
			
			updateColorFromHSV()
		end
		
		if draggingDarkness then
			local mousePos = UserInputService:GetMouseLocation()
			local relativePos = math.clamp((mousePos.X - DarknessBar.AbsolutePosition.X) / DarknessBar.AbsoluteSize.X, 0, 1)
			
			currentDarkness = relativePos
			DarknessDragger.Position = UDim2.new(relativePos, 0, 0.5, 0)
			
			updateColorFromHSV()
		end
	end
end)

function Library:SetNotificationPosition(position)
	local validPositions = {
		TopLeft = true,
		TopRight = true,
		BottomLeft = true,
		BottomRight = true
	}
	
	if validPositions[position] then
		self.NotificationPosition = position
		return true
	else
		warn("Invalid notification position: " .. tostring(position) .. ". Use: TopLeft, TopRight, BottomLeft, or BottomRight")
		return false
	end
end

local activeNotifications = {}

function Library:Notify(options)
	local notifType = options.Type or "Notification"
	local title = options.Title or "Notification"
	local description = options.Description or ""
	local duration = options.Duration or 3
	local position = options.Position or self.NotificationPosition or "BottomRight"
	
	local icons = {
		Error = "rbxassetid://94165803738519",
		Success = "rbxassetid://123509293785756",
		Warning = "rbxassetid://136655865171247",
		Notification = ""
	}
	
	local colors = {
		Error = Color3.fromRGB(239, 68, 68),
		Success = Color3.fromRGB(34, 197, 94),
		Warning = Color3.fromRGB(255, 213, 79),
		Notification = Color3.fromRGB(69, 94, 255)
	}
	
	local positions = {
		TopLeft = {start = UDim2.new(-0.2, 0, 0, 0), target = UDim2.new(0.01, 0, 0.05, 0), direction = 1},
		TopRight = {start = UDim2.new(1.2, 0, 0, 0), target = UDim2.new(0.85, 0, 0.05, 0), direction = 1},
		BottomLeft = {start = UDim2.new(-0.2, 0, 1, 0), target = UDim2.new(0.01, 0, 0.88, 0), direction = -1},
		BottomRight = {start = UDim2.new(1.2, 0, 1, 0), target = UDim2.new(0.85, 0, 0.88, 0), direction = -1}
	}
	
	local pos = positions[position] or positions.BottomRight
	
	if not activeNotifications[position] then
		activeNotifications[position] = {}
	end
	
	local offset = 0
	for _, notif in ipairs(activeNotifications[position]) do
		if notif and notif.Parent then
			offset = offset + (110 * pos.direction)
		end
	end
	
	local notif = createInstance("Frame", {
		Name = "Notification",
		Position = pos.start,
		Size = UDim2.new(0, 280, 0, 100),
		Parent = Neptune,
		BackgroundColor3 = Color3.fromRGB(25, 25, 28),
		BorderSizePixel = 0,
		ZIndex = 200
	})
	
	table.insert(activeNotifications[position], notif)
	
	createInstance("UICorner", {
		Parent = notif,
		CornerRadius = UDim.new(0, 8)
	})
	
	createInstance("UIStroke", {
		Parent = notif,
		Color = Color3.fromRGB(45, 45, 48),
		Thickness = 1,
		Transparency = 0.5
	})
	
	createInstance("TextLabel", {
		Name = "Title",
		Position = UDim2.new(0, 35, 0, 8),
		Size = UDim2.new(1, -45, 0, 24),
		Parent = notif,
		BackgroundTransparency = 1,
		Font = Enum.Font.GothamBold,
		Text = title,
		TextSize = 15,
		TextXAlignment = Enum.TextXAlignment.Left,
		TextYAlignment = Enum.TextYAlignment.Center,
		TextColor3 = Color3.fromRGB(255, 255, 255),
		BorderSizePixel = 0,
		ZIndex = 201
	})
	
	createInstance("Frame", {
		Name = "Line",
		Position = UDim2.new(0, 0, 0, 36),
		Size = UDim2.new(1, 0, 0, 1),
		Parent = notif,
		BackgroundColor3 = Color3.fromRGB(45, 45, 48),
		BorderSizePixel = 0,
		ZIndex = 201
	})
	
	createInstance("TextLabel", {
		Name = "Description",
		Position = UDim2.new(0, 10, 0, 40),
		Size = UDim2.new(1, -20, 0, 42),
		Parent = notif,
		BackgroundTransparency = 1,
		Font = Enum.Font.Gotham,
		Text = description,
		TextSize = 12,
		TextXAlignment = Enum.TextXAlignment.Left,
		TextYAlignment = Enum.TextYAlignment.Top,
		TextWrapped = true,
		TextColor3 = Color3.fromRGB(180, 180, 180),
		BorderSizePixel = 0,
		ZIndex = 201
	})
	
	if icons[notifType] and icons[notifType] ~= "" then
		createInstance("ImageLabel", {
			Name = "Icon",
			Position = UDim2.new(0, 10, 0, 10),
			Size = UDim2.new(0, 18, 0, 18),
			Parent = notif,
			BackgroundTransparency = 1,
			ImageColor3 = Color3.fromRGB(255, 255, 255),
			Image = icons[notifType],
			BorderSizePixel = 0,
			ZIndex = 202
		})
	end
	
	local bar = createInstance("Frame", {
		Name = "Bar",
		Position = UDim2.new(0, 10, 1, -15),
		Size = UDim2.new(1, -20, 0, 6),
		Parent = notif,
		BackgroundColor3 = colors[notifType],
		BorderSizePixel = 0,
		ZIndex = 201
	})
	
	createInstance("UICorner", {
		Parent = bar,
		CornerRadius = UDim.new(1, 0)
	})
	
	local targetPos = UDim2.new(
		pos.target.X.Scale,
		pos.target.X.Offset,
		pos.target.Y.Scale,
		pos.target.Y.Offset + offset
	)
	
	local tweenInfo = TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out)
	local slideIn = TweenService:Create(notif, tweenInfo, {Position = targetPos})
	slideIn:Play()
	
	task.spawn(function()
		task.wait(0.3)
		local barTween = TweenService:Create(bar, TweenInfo.new(duration, Enum.EasingStyle.Linear), {Size = UDim2.new(0, 0, 0, 6)})
		barTween:Play()
		
		task.wait(duration)
		
		local slideOut = TweenService:Create(notif, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.In), {Position = pos.start})
		slideOut:Play()
		
		task.wait(0.4)
		
		for i, v in ipairs(activeNotifications[position]) do
			if v == notif then
				table.remove(activeNotifications[position], i)
				break
			end
		end
		
		notif:Destroy()
		
		local currentOffset = 0
		for _, remainingNotif in ipairs(activeNotifications[position]) do
			if remainingNotif and remainingNotif.Parent then
				local newPos = UDim2.new(
					pos.target.X.Scale,
					pos.target.X.Offset,
					pos.target.Y.Scale,
					pos.target.Y.Offset + currentOffset
				)
				TweenService:Create(remainingNotif, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Position = newPos}):Play()
				currentOffset = currentOffset + (110 * pos.direction)
			end
		end
	end)
end

function Library:CreateTab(name)
	local Tab = {}
	Tab.Name = name
	Tab.Cards = {}
	Tab.LeftCards = {}
	Tab.RightCards = {}
	Tab.RowCount = 0
	
	local tabButton = createInstance("TextButton", {
		Name = "TabButton",
		Size = UDim2.new(0, 160, 0, 45),
		Parent = TabHolder,
		Font = Enum.Font.GothamMedium,
		Text = name,
		TextSize = 16,
		TextXAlignment = Enum.TextXAlignment.Center,
		TextColor3 = Color3.fromRGB(255, 255, 255),
		BackgroundColor3 = Color3.fromRGB(25, 25, 28),
		AutoButtonColor = false,
		BorderSizePixel = 0,
		ZIndex = 1
	})
	
	createInstance("UICorner", {
		Parent = tabButton,
		CornerRadius = UDim.new(0, 5)
	})
	
	Tab.Container = Scroll
	Tab.Button = tabButton
	
	tabButton.MouseButton1Click:Connect(function()
		for _, otherTab in pairs(Library.Tabs) do
			for _, card in pairs(otherTab.Cards) do
				if card.Wrapper then
					card.Wrapper.Visible = false
				end
			end
			if otherTab ~= Tab then
				createTween(otherTab.Button, {BackgroundColor3 = Color3.fromRGB(25, 25, 28)}, 0.2):Play()
			end
		end
		
		for _, card in pairs(Tab.Cards) do
			if card.Wrapper then
				card.Wrapper.Visible = true
			end
		end
		
		createTween(tabButton, {BackgroundColor3 = Color3.fromRGB(69, 94, 255)}, 0.2):Play()
		Library.CurrentTab = Tab
	end)
	
	tabButton.MouseEnter:Connect(function()
		if Library.CurrentTab ~= Tab then
			createTween(tabButton, {BackgroundColor3 = Color3.fromRGB(35, 35, 38)}, 0.2):Play()
		end
	end)
	
	tabButton.MouseLeave:Connect(function()
		if Library.CurrentTab ~= Tab then
			createTween(tabButton, {BackgroundColor3 = Color3.fromRGB(25, 25, 28)}, 0.2):Play()
		end
	end)
	
	if #Library.Tabs == 0 then
		tabButton.BackgroundColor3 = Color3.fromRGB(69, 94, 255)
		Library.CurrentTab = Tab
	end
	
	table.insert(Library.Tabs, Tab)
	
	function Tab:CreateCard(name, side)
	local Card = {}
	Card.Name = name
	Card.Elements = {}
	Card.Side = side or "Left"

	Tab.Rows = Tab.Rows or {}
	Tab.RowCount = Tab.RowCount or 0

	if Card.Side == "Left" then
		Tab.RowCount = Tab.RowCount + 1

		local rowWrapper = createInstance("Frame", {
			Name = "Row_" .. Tab.RowCount,
			Size = UDim2.new(1, 0, 0, 0),
			AutomaticSize = Enum.AutomaticSize.Y,
			Parent = Scroll,
			BackgroundTransparency = 1,
			BorderSizePixel = 0,
			LayoutOrder = Tab.RowCount,
			Visible = Library.CurrentTab == Tab,
			ZIndex = 1
		})

		Tab.Rows[Tab.RowCount] = rowWrapper
	end

	local rowWrapper = Tab.Rows[Tab.RowCount]

	local cardFrame = createInstance("Frame", {
		Name = name,
		Size = UDim2.new(0, 347, 0, 400), 
		AnchorPoint = Card.Side == "Right" and Vector2.new(1, 0) or Vector2.new(0, 0),
		Position = Card.Side == "Right"
			and UDim2.new(1, 0, 0, 0)
			or UDim2.new(0, 0, 0, 0),
		Parent = rowWrapper,
		BackgroundColor3 = Color3.fromRGB(21, 21, 23),
		BorderSizePixel = 0,
		ZIndex = 1
	})
	
	createInstance("UICorner", {
		Parent = cardFrame,
		CornerRadius = UDim.new(0, 4)
	})
	
	local header = createInstance("ImageLabel", {
		Name = "Header",
		Size = UDim2.new(0, 347, 0, 48),
		Parent = cardFrame,
		BackgroundTransparency = 1,
		Image = "rbxassetid://114111795744048",
		BorderSizePixel = 0,
		ZIndex = 1
	})
	
	createInstance("TextLabel", {
		Name = "Title",
		Position = UDim2.new(0.055, 0, 0.150, 0),
		Size = UDim2.new(0, 320, 0, 32),
		Parent = header,
		BackgroundTransparency = 1,
		Font = Enum.Font.GothamMedium,
		Text = name,
		TextSize = 15,
		TextXAlignment = Enum.TextXAlignment.Left,
		TextYAlignment = Enum.TextYAlignment.Center,
		TextColor3 = Color3.fromRGB(255, 255, 255),
		BorderSizePixel = 0,
		ZIndex = 1
	})
	
	
	local mainHolder = createInstance("ScrollingFrame", {
		Name = "MainHolder",
		Position = UDim2.new(0, 0, 0, 48),
		Size = UDim2.new(0, 347, 1, -48),
		Parent = cardFrame,
		BackgroundTransparency = 1,
		ScrollBarThickness = 4,
		ScrollBarImageColor3 = Color3.fromRGB(69, 94, 255),
		CanvasSize = UDim2.new(0, 0, 0, 0),
		BorderSizePixel = 0,
		ZIndex = 1
	})
	
	local holderLayout = createInstance("UIListLayout", {
		Parent = mainHolder,
		Padding = UDim.new(0, 8),
		FillDirection = Enum.FillDirection.Vertical,
		HorizontalAlignment = Enum.HorizontalAlignment.Left,
		VerticalAlignment = Enum.VerticalAlignment.Top,
		SortOrder = Enum.SortOrder.LayoutOrder
	})
	
	createInstance("UIPadding", {
		Parent = mainHolder,
		PaddingTop = UDim.new(0, 10),
		PaddingLeft = UDim.new(0, 10),
		PaddingBottom = UDim.new(0, 10),
		PaddingRight = UDim.new(0, 5)
	})
	
	-- // should update size w/new elements
	holderLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
		mainHolder.CanvasSize = UDim2.new(0, 0, 0, holderLayout.AbsoluteContentSize.Y + 20)
	end)
	
	Card.Frame = cardFrame
	Card.Holder = mainHolder
	Card.Wrapper = rowWrapper
		
		function Card:CreateConfigSystem()
			local configName = ""
			local selectedConfig = ""
			local configDropdown
			
			local nameBox = self:AddTextBox({
				Name = "Config Name",
				Placeholder = "Enter config name...",
				Default = "",
				Callback = function(text)
					configName = text
				end
			})
			
			self:AddButton({
				Name = "Save Config",
				Callback = function()
					if Library:SaveConfig(configName) then
						if configDropdown then
							local configs = Library:GetConfigs()
							if #configs > 0 then
								configDropdown.Refresh(configs)
								configDropdown.Set(configs[#configs])
								selectedConfig = configs[#configs]
							end
						end
					end
				end
			})
			
			local initialConfigs = Library:GetConfigs()
			configDropdown = self:AddDropdown({
				Name = "Select Config",
				List = #initialConfigs > 0 and initialConfigs or {"No configs"},
				Default = initialConfigs[1] or "No configs",
				Callback = function(value)
					selectedConfig = value
				end
			})
			
			if #initialConfigs > 0 then
				selectedConfig = initialConfigs[1]
			end
			
			self:AddButton({
				Name = "Load Config",
				Callback = function()
					if selectedConfig and selectedConfig ~= "No configs" then
						Library:LoadConfig(selectedConfig)
					end
				end
			})
			
			self:AddButton({
				Name = "Delete Config",
				Callback = function()
					if selectedConfig and selectedConfig ~= "No configs" then
						if Library:DeleteConfig(selectedConfig) then
							task.wait(0.1)
							local configs = Library:GetConfigs()
							if #configs > 0 then
								configDropdown.Refresh(configs)
								configDropdown.Set(configs[1])
								selectedConfig = configs[1]
							else
								configDropdown.Refresh({"No configs"})
								configDropdown.Set("No configs")
								selectedConfig = ""
							end
						end
					end
				end
			})
			
			self:AddButton({
				Name = "Refresh List",
				Callback = function()
					local configs = Library:GetConfigs()
					if #configs > 0 then
						configDropdown.Refresh(configs)
						configDropdown.Set(configs[1])
						selectedConfig = configs[1]
					else
						configDropdown.Refresh({"No configs"})
						configDropdown.Set("No configs")
						selectedConfig = ""
					end
					Library:Notify({
						Type = "Notification",
						Title = "Refreshed",
						Description = "Config list updated",
						Duration = 1.5
					})
				end
			})
			
			return {
				Refresh = function()
					local configs = Library:GetConfigs()
					if #configs > 0 then
						configDropdown.Refresh(configs)
					else
						configDropdown.Refresh({"No configs"})
					end
				end
			}
		end
		
		function Card:AddToggle(options)
			local name = options.Name or "Toggle"
			local default = options.Default or false
			local callback = options.Callback or function() end
			local flag = options.Flag
			
			local frame = createInstance("Frame", {
				Name = "Toggle",
				Size = UDim2.new(0, 320, 0, 36),
				Parent = mainHolder,
				BackgroundTransparency = 1,
				BorderSizePixel = 0,
				ZIndex = 1
			})
			
			local toggle = createInstance("TextButton", {
				Position = UDim2.new(0, 5, 0.5, 0),
				AnchorPoint = Vector2.new(0, 0.5),
				Size = UDim2.new(0, 25, 0, 25),
				Parent = frame,
				Text = '',
				BackgroundColor3 = default and Color3.fromRGB(69, 94, 255) or Color3.fromRGB(25, 25, 28),
				AutoButtonColor = false,
				BorderSizePixel = 0,
				ZIndex = 1
			})
			
			createInstance("UICorner", {
				Parent = toggle,
				CornerRadius = UDim.new(0, 5)
			})
			
			local toggleLabel = createInstance("TextLabel", {
				Position = UDim2.new(0, 40, 0.5, 0),
				AnchorPoint = Vector2.new(0, 0.5),
				Size = UDim2.new(0, 270, 0, 36),
				Parent = frame,
				BackgroundTransparency = 1,
				Font = Enum.Font.GothamMedium,
				Text = name,
				TextSize = 14,
				TextXAlignment = Enum.TextXAlignment.Left,
				TextColor3 = Color3.fromRGB(255, 255, 255),
				BorderSizePixel = 0,
				ZIndex = 1,
				Active = false,
				Selectable = false
			})
			
			local isToggled = default
			
			local function updateToggle(value, skipCallback)
				isToggled = value
				local newColor = isToggled and Color3.fromRGB(69, 94, 255) or Color3.fromRGB(25, 25, 28)
				toggle.BackgroundColor3 = newColor
				if flag then
					Library:SetConfigValue(flag, value, updateToggle)
				end
				if not skipCallback then
					callback(isToggled)
				end
			end
			
			toggle.MouseButton1Click:Connect(function()
				updateToggle(not isToggled)
			end)
			
			if flag then
				Library:SetConfigValue(flag, default, updateToggle)
			end
			
			return {
				Set = function(value)
					updateToggle(value, false)
				end
			}
		end
		
		
function Card:AddSlider(options)
	local name = options.Name or "Slider"
	local min = options.Min or 0
	local max = options.Max or 100
	local default = options.Default or 50
	local decimals = options.Decimals or 0
	local callback = options.Callback or function() end
	local flag = options.Flag
	
	local frame = createInstance("Frame", {
		Name = "Slider",
		Size = UDim2.new(0, 320, 0, 55),
		Parent = mainHolder,
		BackgroundTransparency = 1,
		BorderSizePixel = 0,
		ZIndex = 1
	})
	
	createInstance("TextLabel", {
		Position = UDim2.new(0, 5, 0, 0),
		Size = UDim2.new(0, 200, 0, 20),
		Parent = frame,
		BackgroundTransparency = 1,
		Font = Enum.Font.GothamMedium,
		Text = name,
		TextSize = 14,
		TextXAlignment = Enum.TextXAlignment.Left,
		TextColor3 = Color3.fromRGB(255, 255, 255),
		BorderSizePixel = 0,
		ZIndex = 1
	})
	
	local valueBox = createInstance("TextBox", {
		Position = UDim2.new(1, -75, 0, 0),
		Size = UDim2.new(0, 70, 0, 20),
		Parent = frame,
		BackgroundColor3 = Color3.fromRGB(25, 25, 28),
		Font = Enum.Font.Gotham,
		Text = string.format("%." .. decimals .. "f", default),
		TextSize = 13,
		TextXAlignment = Enum.TextXAlignment.Center,
		TextColor3 = Color3.fromRGB(255, 255, 255),
		BorderSizePixel = 0,
		ZIndex = 1
	})
	
	createInstance("UICorner", {
		Parent = valueBox,
		CornerRadius = UDim.new(0, 4)
	})
	
	local holderBar = createInstance("Frame", {
		Position = UDim2.new(0, 5, 0, 30),
		Size = UDim2.new(1, -10, 0, 12),
		Parent = frame,
		BackgroundColor3 = Color3.fromRGB(25, 25, 28),
		BorderSizePixel = 0,
		ZIndex = 1
	})
	
	createInstance("UICorner", {
		Parent = holderBar,
		CornerRadius = UDim.new(0, 5)
	})
	
	local fillBar = createInstance("Frame", {
		Position = UDim2.new(0, 0, 0, 0),
		Size = UDim2.new((default - min) / (max - min), 0, 1, 0),
		Parent = holderBar,
		BackgroundColor3 = Color3.fromRGB(69, 94, 255),
		BorderSizePixel = 0,
		ZIndex = 3
	})
	
	createInstance("UICorner", {
		Parent = fillBar,
		CornerRadius = UDim.new(0, 5)
	})
	
	local dragger = createInstance("TextButton", {
		Position = UDim2.new((default - min) / (max - min), 0, 0.5, 0),
		Size = UDim2.new(0, 10, 0, 22),
		AnchorPoint = Vector2.new(0.5, 0.5),
		Parent = holderBar,
		Text = '',
		BackgroundColor3 = Color3.fromRGB(255, 255, 255),
		AutoButtonColor = false,
		BorderSizePixel = 0,
		ZIndex = 4
	})
	
	createInstance("UICorner", {
		Parent = dragger,
		CornerRadius = UDim.new(0, 5)
	})
	
	local currentValue = default
	local dragging = false
	
	
	local function roundValue(value)
		if decimals == 0 then
			return math.floor(value + 0.5)
		else
			local mult = 10 ^ decimals
			return math.floor(value * mult + 0.5) / mult
		end
	end
	
	local function updateSlider(value, skipCallback)
		value = math.clamp(value, min, max)
		value = roundValue(value)
		currentValue = value
		valueBox.Text = string.format("%." .. decimals .. "f", value)
		local relativePos = (value - min) / (max - min)
		dragger.Position = UDim2.new(relativePos, 0, 0.5, 0)
		fillBar.Size = UDim2.new(relativePos, 0, 1, 0)
		
		if flag then
			Library:SetConfigValue(flag, value, updateSlider)
		end
		if not skipCallback then
			callback(value)
		end
	end
	
	dragger.MouseButton1Down:Connect(function()
		dragging = true
	end)
	
	UserInputService.InputEnded:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 then
			dragging = false
		end
	end)
	
	UserInputService.InputChanged:Connect(function(input)
		if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
			local mousePos = UserInputService:GetMouseLocation()
			local relativePos = math.clamp((mousePos.X - holderBar.AbsolutePosition.X) / holderBar.AbsoluteSize.X, 0, 1)
			local value = min + (max - min) * relativePos
			updateSlider(value, false)
		end
	end)
	
	valueBox.FocusLost:Connect(function()
		local newVal = tonumber(valueBox.Text)
		if newVal then
			updateSlider(newVal, false)
		else
			valueBox.Text = string.format("%." .. decimals .. "f", currentValue)
		end
	end)
	
	if flag then
		Library:SetConfigValue(flag, default, updateSlider)
	end
	
	return {
		Set = function(value)
			updateSlider(value, false)
		end
	}
end
		
		function Card:AddDropdown(options)
			local name = options.Name or "Dropdown"
			local list = options.List or {}
			local default = options.Default or list[1] or ""
			local callback = options.Callback or function() end
			local flag = options.Flag
			
			local frame = createInstance("Frame", {
				Name = "Dropdown",
				Size = UDim2.new(0, 320, 0, 42),
				Parent = mainHolder,
				BackgroundColor3 = Color3.fromRGB(25, 25, 28),
				BorderSizePixel = 0,
				ZIndex = 50
			})
			
			createInstance("UICorner", {
				Parent = frame,
				CornerRadius = UDim.new(0, 4)
			})
			
			local button = createInstance("TextButton", {
				Position = UDim2.new(0, 10, 0.5, 0),
				AnchorPoint = Vector2.new(0, 0.5),
				Size = UDim2.new(1, -20, 0, 32),
				Parent = frame,
				BackgroundTransparency = 1,
				Font = Enum.Font.GothamMedium,
				Text = default,
				TextSize = 14,
				TextXAlignment = Enum.TextXAlignment.Left,
				TextColor3 = Color3.fromRGB(255, 255, 255),
				AutoButtonColor = false,
				BorderSizePixel = 0,
				ZIndex = 51,
				Active = false,
				Selectable = false
			})
			
			local arrow = createInstance("ImageLabel", {
				Position = UDim2.new(1, -25, 0.5, 0),
				AnchorPoint = Vector2.new(0, 0.5),
				Size = UDim2.new(0, 16, 0, 16),
				Parent = button,
				BackgroundTransparency = 1,
				Image = "rbxassetid://134116754541554",
				Rotation = 180,
				ZIndex = 51
			})
			
			local dropContent = createInstance("Frame", {
				Position = UDim2.new(0, 0, 0, 0),
				Size = UDim2.new(0, 320, 0, 0),
				Parent = Neptune,
				BackgroundColor3 = Color3.fromRGB(21, 21, 23),
				ClipsDescendants = true,
				BorderSizePixel = 0,
				Visible = false,
				ZIndex = 999999
			})
			
			createInstance("UICorner", {
				Parent = dropContent,
				CornerRadius = UDim.new(0, 8)
			})
			
			createInstance("UIStroke", {
				Parent = dropContent,
				Color = Color3.fromRGB(69, 94, 255),
				Thickness = 2,
				Transparency = 0.3
			})
			
			local scrollFrame = createInstance("ScrollingFrame", {
				Size = UDim2.new(1, 0, 1, 0),
				Parent = dropContent,
				BackgroundTransparency = 1,
				ScrollBarThickness = 4,
				ScrollBarImageColor3 = Color3.fromRGB(69, 94, 255),
				BorderSizePixel = 0,
				ZIndex = 1000000,
				CanvasSize = UDim2.new(0, 0, 0, 0)
			})
			
			local listLayout = createInstance("UIListLayout", {
				Parent = scrollFrame,
				Padding = UDim.new(0, 6),
				FillDirection = Enum.FillDirection.Vertical,
				HorizontalAlignment = Enum.HorizontalAlignment.Center,
				VerticalAlignment = Enum.VerticalAlignment.Top,
				SortOrder = Enum.SortOrder.LayoutOrder
			})
			
			createInstance("UIPadding", {
				Parent = scrollFrame,
				PaddingTop = UDim.new(0, 8),
				PaddingBottom = UDim.new(0, 8),
				PaddingLeft = UDim.new(0, 10),
				PaddingRight = UDim.new(0, 10)
			})
			
			listLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
				scrollFrame.CanvasSize = UDim2.new(0, 0, 0, listLayout.AbsoluteContentSize.Y + 16)
			end)
			
			local currentValue = default
			local isOpen = false
			local updateConnection
			
			local function updateDropdown(value, skipCallback)
				button.Text = value
				currentValue = value
				if flag then
					Library:SetConfigValue(flag, value, updateDropdown)
				end
				if not skipCallback then
					callback(value)
				end
			end
			
			local function createOptions()
				for _, child in pairs(scrollFrame:GetChildren()) do
					if child:IsA("TextButton") then
						child:Destroy()
					end
				end
				
				for _, option in ipairs(list) do
					local optionBtn = createInstance("TextButton", {
						Size = UDim2.new(1, -20, 0, 36),
						Parent = scrollFrame,
						BackgroundColor3 = Color3.fromRGB(25, 25, 28),
						BackgroundTransparency = 0,
						Font = Enum.Font.GothamMedium,
						Text = option,
						TextSize = 13,
						TextXAlignment = Enum.TextXAlignment.Center,
						TextColor3 = Color3.fromRGB(255, 255, 255),
						AutoButtonColor = false,
						BorderSizePixel = 0,
						ZIndex = 1000001,
						Active = true,
						Selectable = true
					})
					
					createInstance("UICorner", {
						Parent = optionBtn,
						CornerRadius = UDim.new(0, 6)
					})
					
					local highlight = createInstance("Frame", {
						Name = "Highlight",
						Position = UDim2.new(0, 5, 0.5, 0),
						AnchorPoint = Vector2.new(0, 0.5),
						Size = UDim2.new(0, 0, 0, 24),
						Parent = optionBtn,
						BackgroundColor3 = Color3.fromRGB(69, 94, 255),
						BorderSizePixel = 0,
						ZIndex = 1000002
					})
					
					createInstance("UICorner", {
						Parent = highlight,
						CornerRadius = UDim.new(1, 0)
					})
					
					optionBtn.MouseEnter:Connect(function()
						createTween(optionBtn, {
							BackgroundColor3 = Color3.fromRGB(35, 35, 38)
						}, 0.15):Play()
						createTween(highlight, {
							Size = UDim2.new(0, 4, 0, 24)
						}, 0.15):Play()
					end)
					
					optionBtn.MouseLeave:Connect(function()
						createTween(optionBtn, {
							BackgroundColor3 = Color3.fromRGB(25, 25, 28)
						}, 0.15):Play()
						createTween(highlight, {
							Size = UDim2.new(0, 0, 0, 24)
						}, 0.15):Play()
					end)
					
					optionBtn.MouseButton1Click:Connect(function()
						createTween(optionBtn, {
							BackgroundColor3 = Color3.fromRGB(69, 94, 255)
						}, 0.1):Play()
						
						updateDropdown(option, false)
						
						task.wait(0.1)
						
						isOpen = false
						if updateConnection then
							updateConnection:Disconnect()
							updateConnection = nil
						end
						
						createTween(dropContent, {
							Size = UDim2.new(0, 320, 0, 0)
						}, 0.2):Play()
						createTween(arrow, {Rotation = 180}, 0.2):Play()
						
						task.wait(0.2)
						dropContent.Visible = false
					end)
				end
			end
			
			createOptions()
			
			local function updateDropdownPosition()
				local framePos = frame.AbsolutePosition
				local frameSize = frame.AbsoluteSize
				local xPos = framePos.X
				local yPos = framePos.Y + frameSize.Y + 5
				dropContent.Position = UDim2.new(0, xPos, 0, yPos)
			end
			
			frame.MouseEnter:Connect(function()
				if not isOpen then
					createTween(frame, {
						BackgroundColor3 = Color3.fromRGB(30, 30, 33)
					}, 0.2):Play()
				end
			end)
			
			frame.MouseLeave:Connect(function()
				if not isOpen then
					createTween(frame, {
						BackgroundColor3 = Color3.fromRGB(25, 25, 28)
					}, 0.2):Play()
				end
			end)
			
			button.MouseButton1Click:Connect(function()
				if isOpen then
					isOpen = false
					if updateConnection then
						updateConnection:Disconnect()
						updateConnection = nil
					end
					
					createTween(dropContent, {
						Size = UDim2.new(0, 320, 0, 0)
					}, 0.2):Play()
					createTween(arrow, {Rotation = 180}, 0.2):Play()
					
					task.wait(0.2)
					dropContent.Visible = false
				else
					isOpen = true
					local itemCount = #list
					local itemHeight = 36
					local spacing = 6
					local totalItemsHeight = (itemHeight * itemCount) + (spacing * (itemCount - 1))
					local paddingHeight = 16
					local contentHeight = math.min(totalItemsHeight + paddingHeight, 300)
					
					updateDropdownPosition()
					dropContent.Visible = true
					dropContent.Size = UDim2.new(0, 320, 0, 0)
					
					createTween(dropContent, {
						Size = UDim2.new(0, 320, 0, contentHeight)
					}, 0.25):Play()
					createTween(arrow, {Rotation = 0}, 0.25):Play()
					
					if updateConnection then
						updateConnection:Disconnect()
					end
					updateConnection = game:GetService("RunService").Heartbeat:Connect(updateDropdownPosition)
				end
			end)
			
			UserInputService.InputBegan:Connect(function(input, gp)
	if gp then return end
	if input.UserInputType ~= Enum.UserInputType.MouseButton1 then return end
	if not isOpen then return end

	task.defer(function()
		if not isOpen then return end

		local mousePos = UserInputService:GetMouseLocation()
		local dropPos = dropContent.AbsolutePosition
		local dropSize = dropContent.AbsoluteSize
		local buttonPos = frame.AbsolutePosition
		local buttonSize = frame.AbsoluteSize
		
		local inDropdown =
			mousePos.X >= dropPos.X and mousePos.X <= dropPos.X + dropSize.X and
			mousePos.Y >= dropPos.Y and mousePos.Y <= dropPos.Y + dropSize.Y
		
		local inButton =
			mousePos.X >= buttonPos.X and mousePos.X <= buttonPos.X + buttonSize.X and
			mousePos.Y >= buttonPos.Y and mousePos.Y <= buttonPos.Y + buttonSize.Y
		
		if not inDropdown and not inButton then
			isOpen = false
			if updateConnection then
				updateConnection:Disconnect()
				updateConnection = nil
			end
			
			createTween(dropContent, {
				Size = UDim2.new(0, 320, 0, 0)
			}, 0.2):Play()

			createTween(arrow, {Rotation = 180}, 0.2):Play()
			
			task.wait(0.2)
			dropContent.Visible = false
		end
	end)
end)

			
			if flag then
				Library:SetConfigValue(flag, default, updateDropdown)
			end
			
			return {
				Set = function(value)
					updateDropdown(value, false)
				end,
				Refresh = function(newList)
					list = newList
					createOptions()
				end
			}
		end
		
		function Card:AddColorPicker(options)
			local name = options.Name or "Color Picker"
			local default = options.Default or Color3.fromRGB(255, 0, 0)
			local callback = options.Callback or function() end
			local flag = options.Flag
			
			local frame = createInstance("Frame", {
				Name = "ColorPicker",
				Size = UDim2.new(0, 320, 0, 36),
				Parent = mainHolder,
				BackgroundTransparency = 1,
				BorderSizePixel = 0,
				ZIndex = 1
			})
			
			local colorButton = createInstance("TextButton", {
				Position = UDim2.new(0, 5, 0.5, 0),
				AnchorPoint = Vector2.new(0, 0.5),
				Size = UDim2.new(0, 25, 0, 25),
				Parent = frame,
				Text = '',
				BackgroundColor3 = default,
				AutoButtonColor = false,
				BorderSizePixel = 0,
				ZIndex = 1
			})
			
			createInstance("UICorner", {
				Parent = colorButton,
				CornerRadius = UDim.new(0, 5)
			})
			
			createInstance("TextLabel", {
				Position = UDim2.new(0, 40, 0.5, 0),
				AnchorPoint = Vector2.new(0, 0.5),
				Size = UDim2.new(0, 270, 0, 36),
				Parent = frame,
				BackgroundTransparency = 1,
				Font = Enum.Font.GothamMedium,
				Text = name,
				TextSize = 14,
				TextXAlignment = Enum.TextXAlignment.Left,
				TextColor3 = Color3.fromRGB(255, 255, 255),
				BorderSizePixel = 0,
				ZIndex = 1
			})
			
			local function updateColor(color, skipCallback)
				colorButton.BackgroundColor3 = color
				if flag then
					Library:SetConfigValue(flag, {color.R, color.G, color.B}, function(colorData)
						if colorData and type(colorData) == "table" then
							local loadedColor = Color3.new(colorData[1], colorData[2], colorData[3])
							updateColor(loadedColor, true)
						end
					end)
				end
				if not skipCallback then
					callback(color)
				end
			end
			
			colorButton.MouseButton1Click:Connect(function()
				ColorPickerFrame.Visible = not ColorPickerFrame.Visible
				
				currentCallback = function(color)
					updateColor(color, false)
				end
				
				local h, s, v = default:ToHSV()
				currentHue = h * 360
				currentSat = s
				currentVal = v
				currentDarkness = 0
				
				HueDragger.Position = UDim2.new(h, 0, 0.5, 0)
				ColorSelector.Position = UDim2.new(s, 0, 1 - v, 0)
				DarknessDragger.Position = UDim2.new(0, 0, 0.5, 0)
				
				local hueColor = Color3.fromHSV(h, 1, 1)
				Colorview.BackgroundColor3 = hueColor
			end)
			
			if flag then
				Library:SetConfigValue(flag, {default.R, default.G, default.B}, function(colorData)
					if colorData and type(colorData) == "table" then
						local loadedColor = Color3.new(colorData[1], colorData[2], colorData[3])
						updateColor(loadedColor, true)
					end
				end)
			end
			
			return {
				Set = function(color)
					updateColor(color, false)
				end
			}
		end
		
		function Card:AddButton(options)
			local name = options.Name or "Button"
			local callback = options.Callback or function() end
			
			local button = createInstance("TextButton", {
				Size = UDim2.new(0, 320, 0, 36),
				Parent = mainHolder,
				BackgroundColor3 = Color3.fromRGB(69, 94, 255),
				Font = Enum.Font.GothamMedium,
				Text = name,
				TextSize = 14,
				TextColor3 = Color3.fromRGB(255, 255, 255),
				AutoButtonColor = false,
				BorderSizePixel = 0,
				ZIndex = 1
			})
			
			createInstance("UICorner", {
				Parent = button,
				CornerRadius = UDim.new(0, 5)
			})
			
			button.MouseEnter:Connect(function()
				createTween(button, {BackgroundColor3 = Color3.fromRGB(89, 114, 255)}, 0.2):Play()
			end)
			
			button.MouseLeave:Connect(function()
				createTween(button, {BackgroundColor3 = Color3.fromRGB(69, 94, 255)}, 0.2):Play()
			end)
			
			button.MouseButton1Click:Connect(function()
				createTween(button, {Size = UDim2.new(0, 315, 0, 34)}, 0.1):Play()
				task.wait(0.1)
				createTween(button, {Size = UDim2.new(0, 320, 0, 36)}, 0.1):Play()
				callback()
			end)
		end
		
		function Card:AddKeybind(options)
			local name = options.Name or "Keybind"
			local default = options.Default or Enum.KeyCode.E
			local callback = options.Callback or function() end
			local flag = options.Flag
			
			local frame = createInstance("Frame", {
				Name = "Keybind",
				Size = UDim2.new(0, 320, 0, 36),
				Parent = mainHolder,
				BackgroundTransparency = 1,
				BorderSizePixel = 0,
				ZIndex = 1
			})
			
			createInstance("TextLabel", {
				Position = UDim2.new(0, 5, 0.5, 0),
				AnchorPoint = Vector2.new(0, 0.5),
				Size = UDim2.new(0, 200, 0, 36),
				Parent = frame,
				BackgroundTransparency = 1,
				Font = Enum.Font.GothamMedium,
				Text = name,
				TextSize = 14,
				TextXAlignment = Enum.TextXAlignment.Left,
				TextColor3 = Color3.fromRGB(255, 255, 255),
				BorderSizePixel = 0,
				ZIndex = 1
			})
			
			local keybindButton = createInstance("TextButton", {
				Position = UDim2.new(1, -75, 0.5, 0),
				AnchorPoint = Vector2.new(0, 0.5),
				Size = UDim2.new(0, 70, 0, 26),
				Parent = frame,
				BackgroundColor3 = Color3.fromRGB(25, 25, 28),
				Font = Enum.Font.Gotham,
				Text = default.Name,
				TextSize = 12,
				TextColor3 = Color3.fromRGB(255, 255, 255),
				AutoButtonColor = false,
				BorderSizePixel = 0,
				ZIndex = 1,
				Active = false,
				Selectable = false
			})
			
			createInstance("UICorner", {
				Parent = keybindButton,
				CornerRadius = UDim.new(0, 5)
			})
			
			local currentKey = default
			local listening = false
			
			local function updateKeybind(key, skipCallback)
				currentKey = key
				keybindButton.Text = key.Name
				if flag then
					Library:SetConfigValue(flag, key.Name, function(keyName)
						local keyCode = Enum.KeyCode[keyName]
						if keyCode then
							updateKeybind(keyCode, true)
						end
					end)
				end
				if not skipCallback then
					callback(key)
				end
			end
			
			keybindButton.MouseButton1Click:Connect(function()
				listening = true
				keybindButton.Text = "..."
				keybindButton.BackgroundColor3 = Color3.fromRGB(69, 94, 255)
			end)
			
			UserInputService.InputBegan:Connect(function(input, gameProcessed)
				if listening then
					if input.UserInputType == Enum.UserInputType.Keyboard then
						updateKeybind(input.KeyCode, false)
						keybindButton.BackgroundColor3 = Color3.fromRGB(25, 25, 28)
						listening = false
					end
				elseif not gameProcessed and input.KeyCode == currentKey then
					callback(currentKey)
				end
			end)
			
			if flag then
				Library:SetConfigValue(flag, default.Name, function(keyName)
					local keyCode = Enum.KeyCode[keyName]
					if keyCode then
						updateKeybind(keyCode, true)
					end
				end)
			end
			
			return {
				Set = function(keyCode)
					updateKeybind(keyCode, false)
				end,
				Get = function()
					return currentKey
				end
			}
		end
		
		function Card:AddTextBox(options)
			local name = options.Name or "TextBox"
			local placeholder = options.Placeholder or "Enter text..."
			local default = options.Default or ""
			local callback = options.Callback or function() end
			local flag = options.Flag
			
			local frame = createInstance("Frame", {
				Name = "TextBox",
				Size = UDim2.new(0, 320, 0, 55),
				Parent = mainHolder,
				BackgroundTransparency = 1,
				BorderSizePixel = 0,
				ZIndex = 1
			})
			
			createInstance("TextLabel", {
				Position = UDim2.new(0, 5, 0, 0),
				Size = UDim2.new(0, 200, 0, 20),
				Parent = frame,
				BackgroundTransparency = 1,
				Font = Enum.Font.GothamMedium,
				Text = name,
				TextSize = 14,
				TextXAlignment = Enum.TextXAlignment.Left,
				TextColor3 = Color3.fromRGB(255, 255, 255),
				BorderSizePixel = 0,
				ZIndex = 1
			})
			
			local textBox = createInstance("TextBox", {
				Position = UDim2.new(0, 5, 0, 25),
				Size = UDim2.new(1, -10, 0, 26),
				Parent = frame,
				BackgroundColor3 = Color3.fromRGB(25, 25, 28),
				Font = Enum.Font.Gotham,
				PlaceholderText = placeholder,
				Text = default,
				TextSize = 13,
				TextXAlignment = Enum.TextXAlignment.Left,
				TextColor3 = Color3.fromRGB(255, 255, 255),
				PlaceholderColor3 = Color3.fromRGB(120, 120, 120),
				ClearTextOnFocus = false,
				BorderSizePixel = 0,
				ZIndex = 1,
				Active = false,
				Selectable = false
			})
			
			createInstance("UICorner", {
				Parent = textBox,
				CornerRadius = UDim.new(0, 5)
			})
			
			createInstance("UIPadding", {
				Parent = textBox,
				PaddingLeft = UDim.new(0, 10),
				PaddingRight = UDim.new(0, 10)
			})
			
			local function updateTextBox(text, skipCallback)
				textBox.Text = text
				if flag then
					Library:SetConfigValue(flag, text, updateTextBox)
				end
				if not skipCallback then
					callback(text)
				end
			end
			
			textBox.FocusLost:Connect(function(enterPressed)
				updateTextBox(textBox.Text, false)
			end)
			
			if flag then
				Library:SetConfigValue(flag, default, updateTextBox)
			end
			
			return {
				Set = function(text)
					updateTextBox(text, false)
				end,
				Get = function()
					return textBox.Text
				end
			}
		end
		
		table.insert(Tab.Cards, Card)
		return Card
	end
	
	return Tab
end

task.spawn(function()
	wait(1)
    local ver = game:GetService("CoreGui").RobloxGui.SettingsClippingShield.SettingsShield.VersionContainer.PlaceVersionLabel
    local cleanedText = ver.Text:gsub("^Place%s*", "")
    local version = cleanedText:match("(%d+)")
    if version == "480" then
        Library:Notify({
	        Type = "Success",
	        Title = "Valid Version",
	        Description = "Neptune Hub Loaded!",
	        Duration = 3
        })
	elseif version == "53" then
		Library:Notify({
	        Type = "Success",
	        Title = "Valid Version",
	        Description = "Neptune Hub Loaded! Practice Version.",
	        Duration = 3
		})
    elseif version > "480" then
        Library:Notify({
	        Type = "Error",
	        Title = "Invalid Version",
	        Description = "You are on an updated version" .. ver .. "Please be careful using the script!",
	        Duration = 3
        })
	elseif version < "480" then
        Library:Notify({
	        Type = "Error",
	        Title = "Invalid Version",
	        Description = "You are on an outdated version" .. ver .. "Please be careful using the script!",
	        Duration = 3
        })
    end
end)

Library.ToggleKey = Enum.KeyCode.LeftControl

return Library
